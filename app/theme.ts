"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#402011",
    },
    secondary: {
      main: "#605B58",
    },
    text: {
      primary: "#F4F2E4",
    },
  },
});

export default theme;
